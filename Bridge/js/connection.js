/*
 *
 *
*/

function Auth();
function Control(SCOPE);

var app = angular.module('app-gateway', []);
app.controller('ctrl-gateway', Control($scope))

function Auth() {

}

function Control(SCOPE) {
  
}
